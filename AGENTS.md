# AGENTS.md

## 项目目标

队伍名称：维拉星核。项目名称：VelaBridge。仓库名称：`contest2026_039_weilaxinghe`。比赛：2026 首届 openvela AI 硬件开发者大赛。

维拉星核团队开发的 VelaBridge，是一款基于 openvela 的多模态无障碍 AI 沟通与伴行终端。

项目面向听障、视障、语言表达困难者，在校园问路、窗口服务、食堂点餐、读门牌、一键求助等场景中，把外界语音、文字和用户按键事件转换为字幕、语音播报、震动反馈和快捷回复。

第一阶段目标是可落地 MVP，不做复杂外壳、复杂室内导航、完整离线大模型、医疗诊断或替代导盲功能。

## MVP 优先级

1. 串口 JSON Lines 通信：电脑端 AI Bridge 与 openvela 设备端互发结构化消息。
2. 字幕显示：优先支持短文本、分步骤简化字幕和危险提醒。
3. 按键状态机：短按、长按、双击等事件先进入统一接口。
4. 震动反馈：支持 `none`、`short`、`long`、`urgent`、`double`。
5. 快捷回复：用固定回复降低听障 / 表达困难用户的沟通成本。
6. 一键求助：生成求助摘要并进入 HELP_MODE。
7. 日志：记录脱敏文字摘要、模式变化、按键事件和错误码。
8. 演示模式：支持固定比赛演示脚本，保证现场可复现。

## 禁止事项

- 不要声称 VelaBridge 可以替代医疗设备。
- 不要声称 VelaBridge 可以替代导盲杖或导盲犬。
- 不要默认保存原始音频、原始图片或未经授权的敏感数据。
- 不要添加昂贵、稀缺或比赛现场难以复现的硬件依赖。
- 不要把第一阶段项目扩展成万能导盲机器人或完整安全监护系统。
- 不要接入真实 API Key、密钥或个人隐私数据。

## 代码规范

- 小步提交，提交信息写清楚改动范围。
- 保留现有测试，新增逻辑尽量配套最小可运行测试。
- TODO 必须写清楚原因、边界和下一步验证方式。
- 尽量不破坏现有 `app/hello_app`、`quickapp/hello_quickapp` 和 `board/contest_board`。
- 不随意修改 `openvela.xml`；新增作品映射优先修改队伍 manifest，并说明原因。
- Python mock 优先使用标准库，不强制依赖外部包。
- 设备端 C 代码先使用 log stub，不乱写未确认的屏幕、按键、震动、音频驱动调用。

## Codex 工作要求

每次变更必须说明：

- 修改了哪些文件。
- 为什么修改。
- 如何测试，包括命令和结果。
- 哪些内容需要人工接开发板验证。
- 是否修改 manifest；如果修改，说明映射关系和原因。
