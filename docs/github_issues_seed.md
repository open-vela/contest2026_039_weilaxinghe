# VelaBridge MVP GitHub Issues Seed

当前环境已检测到 `gh` CLI，但 `gh auth status` 显示未登录，因此未直接创建 GitHub Issues。下面内容可复制到 GitHub Issues 中逐条创建。

团队名称：维拉星核。项目名称：VelaBridge。比赛编号：039。

正式表述：维拉星核团队开发的 VelaBridge，是一款基于 openvela 的多模态无障碍 AI 沟通与伴行终端。

## 1. P0: openvela 开发环境与黄山派基础运行

**标题**：P0: openvela 开发环境与黄山派基础运行

**团队名称**：维拉星核

**背景**：VelaBridge 后续设备端功能必须先确认完整 openvela 工作区、黄山派开发板和基础运行链路可用。

**任务范围**：
- 按官方 README 拉取完整 openvela 工程。
- 确认 `build.sh`、目标 board config 和烧录 / 运行入口。
- 记录黄山派基础启动、串口日志查看方式和环境坑点。

**验收标准**：
- 能在本机找到完整 openvela 工作区和 `build.sh`。
- 明确黄山派使用的 board config 路径。
- 有一段可复现的基础运行 / 日志查看说明。

**需要修改的目录**：`README.md`、`docs/`

**是否需要开发板**：是

**优先级**：P0

**建议负责人角色**：固件工程师

## 2. P0: VelaBridge 串口 JSON 协议

**标题**：P0: VelaBridge 串口 JSON 协议

**团队名称**：维拉星核

**背景**：设备端和电脑端 AI Bridge 需要用稳定的 JSON Lines 协议交换字幕、按键、震动和日志事件。

**任务范围**：
- 评审 `protocol/velabridge_protocol.md` 字段、消息类型、模式枚举和错误码。
- 明确 `bridge_to_device` 与 `device_to_bridge` 的最小必填字段。
- 准备 3-5 条可用于串口联调的 JSONL 样例。

**验收标准**：
- 协议文档能指导设备端和 AI Bridge 各自实现。
- 明确非法 JSON、未知类型、文本过长、设备忙、AI 超时的处理方式。
- JSON 示例能被标准 JSON parser 解析。

**需要修改的目录**：`protocol/`、`docs/`

**是否需要开发板**：否

**优先级**：P0

**建议负责人角色**：协议 / 固件工程师

## 3. P0: AI Bridge mock 脚本

**标题**：P0: AI Bridge mock 脚本

**团队名称**：维拉星核

**背景**：第一阶段不接真实 ASR/OCR/LLM/API Key，先用可复现 mock 输出 VelaBridge JSONL。

**任务范围**：
- 完善 `ai_bridge/mock_bridge.py` 的命令行输入、`--demo` 和 `--jsonl`。
- 保持 Python 标准库优先，不强制外部依赖。
- 维护危险关键词、快捷回复、求助、盲人菜单、OCR mock 和演示步骤规则。

**验收标准**：
- `python3 ai_bridge/mock_bridge.py --demo --jsonl` 可输出 5-7 条 JSON Lines。
- `python3 -m unittest tests/test_mock_bridge.py` 通过。
- 不读取或写入任何真实 API Key。

**需要修改的目录**：`ai_bridge/`、`tests/`

**是否需要开发板**：否

**优先级**：P0

**建议负责人角色**：AI 工程师

## 4. P0: 屏幕字幕显示接口

**标题**：P0: 屏幕字幕显示接口

**团队名称**：维拉星核

**背景**：听障字幕是 VelaBridge 的核心闭环，设备端需要先定义最小显示接口。

**任务范围**：
- 在设备端应用中预留 `caption.display` 显示接口。
- 支持普通字幕和分步骤简化字幕。
- 先实现清晰的 stub / 日志输出，真实屏幕驱动调用需要开发板 API 确认。

**验收标准**：
- 收到 `caption` 消息后能进入字幕显示路径。
- 显示内容优先使用 `display`，缺省时回退到 `text`。
- 长文本有截断或换行策略说明。

**需要修改的目录**：`app/velabridge_app/`、`protocol/`

**是否需要开发板**：是

**优先级**：P0

**建议负责人角色**：固件工程师

## 5. P0: 按键状态机

**标题**：P0: 按键状态机

**团队名称**：维拉星核

**背景**：VelaBridge 需要用少量实体按键完成模式切换、快捷回复和求助触发。

**任务范围**：
- 定义短按、长按、双击事件。
- 定义 `NORMAL`、`CAPTION_MODE`、`BLIND_MODE`、`OCR_MODE`、`HELP_MODE`、`DEMO_MODE` 的最小切换规则。
- 先用日志或模拟事件验证状态流转。

**验收标准**：
- 每种按键事件都有明确处理函数或接口。
- 状态切换能输出可读日志。
- 长按进入 HELP_MODE 的规则明确。

**需要修改的目录**：`app/velabridge_app/`、`protocol/`

**是否需要开发板**：是

**优先级**：P0

**建议负责人角色**：固件工程师

## 6. P0: 震动反馈模式

**标题**：P0: 震动反馈模式

**团队名称**：维拉星核

**背景**：震动反馈用于非视觉、非听觉提示，尤其适合危险提醒、模式切换和求助确认。

**任务范围**：
- 支持 `none`、`short`、`long`、`urgent`、`double` 五种协议枚举。
- 先建立震动接口和日志 stub。
- 开发板确认后再接入真实震动硬件 API。

**验收标准**：
- 收到不同 `vibration` 值时能走到对应处理路径。
- 未知震动枚举有降级策略。
- `danger_alert` 默认映射到 `urgent`。

**需要修改的目录**：`app/velabridge_app/`、`protocol/`

**是否需要开发板**：是

**优先级**：P0

**建议负责人角色**：硬件 / 固件工程师

## 7. P0: 快捷回复流程

**标题**：P0: 快捷回复流程

**团队名称**：维拉星核

**背景**：快捷回复帮助听障或语言表达困难用户快速表达“请说慢一点”等常用句。

**任务范围**：
- 在 AI Bridge mock 中保持 `quick_reply` 输出。
- 设备端接收 `quick_reply` 后显示文本并预留播报接口。
- 明确按键触发快捷回复的后续接入点。

**验收标准**：
- mock 输入“请说慢一点”能输出 `quick_reply`。
- 设备端设计能区分显示文本和播报文本。
- 不依赖真实 TTS 也能完成演示。

**需要修改的目录**：`ai_bridge/`、`tests/`、`app/velabridge_app/`

**是否需要开发板**：是

**优先级**：P0

**建议负责人角色**：产品 / 固件工程师

## 8. P0: 一键求助流程

**标题**：P0: 一键求助流程

**团队名称**：维拉星核

**背景**：一键求助是安全相关但不替代安全设备的 MVP 能力，需要边界清楚、触发明确。

**任务范围**：
- AI Bridge mock 支持“求助”“帮我联系老师”触发 `help`。
- 设备端长按进入 HELP_MODE。
- 输出求助摘要、震动提示和串口日志。

**验收标准**：
- `help` 消息优先级为 high。
- 设备端有 HELP_MODE 入口和退出策略。
- 文档明确不自动上传敏感信息，不替代医疗或安全设备。

**需要修改的目录**：`ai_bridge/`、`protocol/`、`app/velabridge_app/`、`docs/`

**是否需要开发板**：是

**优先级**：P0

**建议负责人角色**：产品 / 固件工程师

## 9. P1: 盲人模式语音菜单

**标题**：P1: 盲人模式语音菜单

**团队名称**：维拉星核

**背景**：盲人模式用于无屏或低视觉场景下的菜单提示，但第一阶段不替代导盲功能。

**任务范围**：
- 保持 `blind_menu` 协议消息和 mock 输出。
- 定义短按下一项、长按确认、双击退出的菜单交互。
- 先用文本日志或播报字段模拟语音菜单。

**验收标准**：
- mock 能输出 `blind_menu`。
- 菜单交互不依赖真实语音合成也能演示。
- 文档明确不替代导盲杖或导盲犬。

**需要修改的目录**：`ai_bridge/`、`protocol/`、`app/velabridge_app/`、`docs/`

**是否需要开发板**：是

**优先级**：P1

**建议负责人角色**：产品 / 固件工程师

## 10. P1: OCR 读字 mock

**标题**：P1: OCR 读字 mock

**团队名称**：维拉星核

**背景**：OCR 读字用于演示读门牌、窗口文字、菜单文字，第一阶段先用 mock 验证消息链路。

**任务范围**：
- AI Bridge mock 输入“读字”“门牌”等关键词时输出 `ocr_result`。
- 协议中明确 OCR 结果的 `display` 和 `speak` 用法。
- 设备端预留 OCR_MODE 显示 / 播报路径。

**验收标准**：
- mock 能稳定输出 `ocr_result`。
- unittest 覆盖 OCR mock 基本路径。
- 不接真实摄像头，不保存原始图片。

**需要修改的目录**：`ai_bridge/`、`tests/`、`protocol/`

**是否需要开发板**：否

**优先级**：P1

**建议负责人角色**：AI 工程师

## 11. P1: DEMO_MODE 固定演示脚本

**标题**：P1: DEMO_MODE 固定演示脚本

**团队名称**：维拉星核

**背景**：比赛演示需要稳定复现，避免现场依赖真实 AI 服务或网络。

**任务范围**：
- 维护 `--demo --jsonl` 的固定 5-7 步消息。
- 将 demo 步骤与 `docs/demo_script.md` 对齐。
- 设备端预留 DEMO_MODE 接收和展示路径。

**验收标准**：
- demo 输出不少于 5 条、不超过 7 条消息。
- 包含字幕、快捷回复、盲人菜单、OCR、危险提醒。
- 演示脚本可在 5 分钟视频中直接使用。

**需要修改的目录**：`ai_bridge/`、`tests/`、`docs/`

**是否需要开发板**：否

**优先级**：P1

**建议负责人角色**：产品 / AI 工程师

## 12. P1: AI Coding 日志与提交物整理

**标题**：P1: AI Coding 日志与提交物整理

**团队名称**：维拉星核

**背景**：比赛评审会参考作品说明和 `logs/` 中的 AI Coding 日志，需要按官方格式整理。

**任务范围**：
- 根据 `logs/README.md` 明确真实日志导出路径。
- 使用 `logs/ai_coding_log.md` 记录人工审核、测试结果和风险 TODO。
- 检查 README、协议、演示脚本、测试命令是否同步。

**验收标准**：
- 每次主要 AI 协作都有可追踪记录。
- README 能说明如何运行 mock 和测试。
- 不提交密钥、原始音频、原始图片或未授权隐私数据。

**需要修改的目录**：`logs/`、`README.md`、`docs/`

**是否需要开发板**：否

**优先级**：P1

**建议负责人角色**：项目负责人
